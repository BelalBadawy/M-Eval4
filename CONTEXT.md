# Performance Evaluation System

Manages employee performance evaluations: organizational hierarchy, evaluation cycles,
versioned evaluation groups, dynamic evaluation forms, forced distribution, and approval.

This glossary is the project's shared vocabulary. It is not a spec — behaviour lives in
`PRD.md`, decisions in `.agents/adr/`.
